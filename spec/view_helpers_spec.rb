require File.dirname(__FILE__) + '/spec_helper'

describe WillPaginate::Liquorized::ViewHelpers do
  
  before(:each) do
    # setup
  end

  it "should render pagination successfully from liquor template"
  it "should respect will paginate options (anchor and link labels)"
  
end